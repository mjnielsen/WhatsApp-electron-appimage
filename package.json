{
  "name": "WhatsApp",
  "version": "1.0.0",
  "description": "WhatsApp",
  "main": "main.js",
  "devDependencies": {
    "electron": "^1.8.6",
    "electron-packager": "^12.0.2"
  },
  "scripts": {
    "package-linux": "electron-packager . WhatsApp --overwrite --asar --platform=linux --arch=x64 --icon=assets/icons/png/256x256.png --prune=true --out=release_builds"
  }
}
